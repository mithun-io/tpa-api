git init -b main

git config --global user.name "mithun-io"
git config --global user.email "mithun-io@outlook.com"
git config --global -l

git remote add origin https://github.com/mithun-io/tpa
git remote -v

git add .
git commit -m "initial commit"

git push -u origin main

made changes:
git add .
git commit -m "commit message"
git push

// workflow
git clone https://github.com/mithun-io/tpa-api.git

cd tpa/

git checkout -b dev

git add .
git commit -m "commit message"

git push origin dev
 
notification pops up on the repository - https://github.com/mithun-io/tpa-api
click it and review for pull request and merge finally
