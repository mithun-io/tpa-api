git init -b main

git config --global user.name "mithun-io"
git config --global user.email "mithun-io@outlook.com"
git config --global -l

git remote add origin https://github.com/mithun-io/tpa
git remote -v

git add .
git commit -m "initial commit"

git push -u origin main

made changes:
git add .
git commit -m "commit message"
git push

// workflow
git clone 

cd <repo>

git checkout -b <name>

git add .
git commit -m " "

git push origin <name>
 
goto repo you will get a notification for a pull request
give a name, and review it
